# Tests for the lit configuration itself. They are plain Python and need
# neither a GPU nor the Python bindings, so this directory has no gating beyond
# enabling .py discovery.
config.suffixes.add(".py")
config.excludes.add("__pycache__")
